# 智能体设计说明书

## 教育智能体赛道-课答-陈正洋

---

## 一、设计思路

### 1.1 问题背景

高校学生在课后学习中面临三个核心痛点：

- **答疑不及时**：遇到教材或课件中的疑问，依赖教师答疑时间或同学讨论，反馈周期长；
- **答案不可溯源**：通用 AI 助手（ChatGPT 等）虽能回答，但无法区分"基于课件内容"还是"模型自身常识"，学生无从核实；
- **学习资料碎片化**：教材、PPT、笔记分散在不同格式、不同设备中，难以高效检索利用。

### 1.2 设计理念

课答（Kecap）的核心设计理念是 **"资料为锚，AI 为桥"**——

- **资料为锚**：所有回答以学生上传的课程资料为知识基础，逐句标注出处（文档名 + 页码），让 AI 的答案"可追溯、可验证"；
- **AI 为桥**：当课程资料不够详尽时，AI 用自己的知识自然补充背景原理、推导过程、类比举例，不因资料缺失而拒绝回答，也不因完全依赖资料而给出"干瘪"结论。

这一设计平衡了 **检索增强生成（RAG）的可靠性** 与 **大语言模型（LLM）的灵活性**，形成"优先基于资料 → 智能补充扩展 → 资料无关时纯通用回答"的三级回答策略。

---

## 二、整体架构

### 2.1 系统架构图

```
┌─────────────────────────────────────────────────────┐
│                    用户（浏览器）                      │
│              http://localhost:8000                    │
└─────────────────────┬───────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────┐
│              前端层（Next.js 14 静态导出）              │
│   Chat UI · 课程管理 · 文档上传 · 对话流式渲染(SSE)     │
└─────────────────────┬───────────────────────────────┘
                      │ REST / SSE
                      ▼
┌─────────────────────────────────────────────────────┐
│              应用层（FastAPI + Uvicorn）               │
│                                                     │
│  ┌──────────┐  ┌──────────┐  ┌──────────────────┐  │
│  │ 课程管理  │  │ 文档上传  │  │   智能答疑(RAG)   │  │
│  │ courses  │  │ upload   │  │   chat/ask       │  │
│  └──────────┘  └────┬─────┘  └────────┬─────────┘  │
│                     │                 │              │
│                     ▼                 ▼              │
│  ┌──────────────────────────────────────────────┐   │
│  │              RAG 引擎层                        │   │
│  │                                              │   │
│  │  文档解析 ──► 智能分块 ──► 向量化 ──► Qdrant   │   │
│  │  (多格式)    (语义边界)   (BGE-M3)   存储      │   │
│  │                                              │   │
│  │  Query扩展 ──► 混合检索 ──► 重排序 ──► LLM生成 │   │
│  └──────────────────────────────────────────────┘   │
└─────────────────────┬───────────────────────────────┘
                      │
          ┌───────────┼───────────┐
          ▼           ▼           ▼
    ┌──────────┐ ┌───────┐ ┌───────────┐
    │  SQLite   │ │Qdrant │ │ DeepSeek   │
    │(业务数据) │ │(向量) │ │ API (LLM)  │
    └──────────┘ └───────┘ └───────────┘
```

### 2.2 部署模式

课答支持三种部署模式，适应不同使用场景：

| 模式 | 适用场景 | 数据库 | 向量库 | 前端 |
|------|---------|--------|--------|------|
| **EXE 一键版** | 普通用户，Windows 桌面 | SQLite（本地文件） | Qdrant（本地文件） | FastAPI 静态托管 |
| **开发模式** | 代码开发调试 | SQLite | Qdrant 本地模式 | Next.js Dev Server (:3000) |
| **Docker 模式** | 服务器部署 | PostgreSQL | Qdrant 容器 | FastAPI 静态托管 |

### 2.3 关键技术选型

| 层级 | 技术 | 选择理由 |
|------|------|---------|
| 前端 | Next.js 14 + Tailwind CSS | 支持 SSG 静态导出，可打包进 EXE；Tailwind 零运行时 |
| 后端 | Python FastAPI | 异步高性能、原生 SSE 流式支持、自动 OpenAPI 文档 |
| LLM | DeepSeek-V3 API | 中文能力强、OpenAI SDK 兼容、成本低于 GPT-4 的 1/10 |
| Embedding | 硅基流动 BGE-M3 | 1024 维稠密向量，中英双语语义对齐能力强，支持 8192 上下文 |
| 向量数据库 | Qdrant | Rust 高性能实现，支持本地文件模式（免 Docker，EXE 打包友好） |
| 业务数据库 | SQLite / PostgreSQL | 开发用 SQLite 零配置；生产 Docker 自动切 PostgreSQL |
| 打包 | PyInstaller onefile | 单文件 67MB，双击运行，零安装门槛 |

---

## 三、功能模块

### 3.1 课程管理模块

- **创建课程**：按学科/科目组织知识库，如"数据结构"、"数据库原理"
- **课程列表**：展示课程名称、文档数量、分块数量、创建时间
- **删除课程**：级联删除关联文档、向量数据、文件存储

### 3.2 文档上传与处理模块

- **多格式支持**：PDF（PyMuPDF）、PPTX（python-pptx）、DOCX（python-docx）、Markdown、TXT
- **自动处理流水线**：
  1. 文件格式校验（允许扩展名白名单，最大 50MB）
  2. 文本提取（保留页码/幻灯片号）
  3. 智能分块（800 字符/块，150 字符重叠，按段落/标题边界切分）
  4. 向量化（BGE-M3, 1024 维，Cosine 距离）
  5. 写入 Qdrant + 业务数据库

### 3.3 RAG 智能答疑模块

- **完整检索链路**：Query 扩展 → 向量检索（Top-10）→ 按向量分数排序（Top-3）→ LLM 生成
- **三级回答策略**：
  - 资料相关 → 基于资料，句末标注 [编号]
  - 资料不够详细 → 资料为主 + AI 知识自然补充
  - 资料无关 → 纯通用知识回答，不强制拉扯资料
- **流式输出（SSE）**：逐 token 推送，首字延迟 < 2 秒
- **会话管理**：自动保存对话历史，最近 10 条带入 LLM 上下文

### 3.4 引文溯源模块

- 每条 LLM 回答附带 **参考来源清单**，格式：`[编号] 文档名 (页码/位置)`
- 前端引用面板实时展示：引用文本摘要、来源文档、页码、相关度分数

### 3.5 EXE 打包与一键部署

- PyInstaller 将 Python 后端 + Next.js 静态前端打包为单个 `课答.exe`（67MB）
- 数据目录（SQLite + Qdrant + 上传文件）全部放在 EXE 同级目录，方便拷贝备份
- 双重保护：端口检测防止重复启动（Qdrant 本地模式不支持多实例）；.env 错误容忍（缺失时使用默认值启动）

---

## 四、算法原理

### 4.1 智能分块算法

课答的分块算法不是简单的按字符数切分，而是模拟"人类阅读"的语义边界：

```
输入：全文 text
1. 按页面/幻灯片分隔符（{PAGE:n} / {SLIDE:n}）标记段落边界
2. 以双换行（\n\n）为段落分割单位
3. 逐段落累加：
   - 若当前 chunk + 新段落 ≤ chunk_size(800) → 追加到当前 chunk
   - 若超出 → 保存当前 chunk，重叠 overlap(150) 字符作为新 chunk 前缀
4. 记录每个 chunk 的起始页码（引用标注用）
5. 输出：[{id, content, chunk_index, page_number}, ...]
```

**关键优化**：页码标记用双换行隔离（`\n\n{PAGE:n}\n\n`），避免标记与正文段落粘连导致正文被误丢弃。

### 4.2 Query 扩展算法

```
输入：原始提问 query
1. 保留原始 query
2. 用正则 [，。！？；：、\n] 切分子句
3. 过滤长度 ≤ 3 的子句和重复项
4. 上限 3 条子 Query
5. 对每条子 Query 独立执行向量检索
6. 按 chunk_id 合并去重（保留得分更高的）
7. 输出：去重后的文档列表，按向量相似度降序
```

### 4.3 向量检索

采用 Qdrant 稠密向量检索：

- **Embedding 模型**：BAAI/bge-m3，输出 1024 维浮点向量
- **距离度量**：Cosine 相似度
- **Course 隔离**：检索时加 `course_id` 过滤，不同课程的知识库互相隔离
- **分数阈值**：score ≥ 0.35，过滤低相关度噪音

### 4.4 重排序（Reranker 可选）

- **模型**：BAAI/bge-reranker-v2-m3（Cross-encoder 架构）
- **策略**：Query-Document pair 联合编码，精确计算语义相关度
- **当前状态**：由于 BGE-Reranker-v2-m3 对中文学术文本排序效果不稳定，当前策略为**按向量得分降序取 Top-3**，Reranker 作为可选增强模块待后续优化
- **HuggingFace 镜像**：通过 `HF_ENDPOINT=https://hf-mirror.com` 解决国内下载阻断

### 4.5 答案生成

```
输入：问题 + 检索文档片段 + 对话历史(最近10条)
1. 构建含检索上下文的 Prompt（标注来源文档名、页码）
2. System Prompt 定义三级回答策略
3. 调用 DeepSeek-V3 (deepseek-chat, temperature=0.3, max_tokens=2048)
4. 解析回答中的引用编号 [1]、[2]
5. 计算置信度：top_score × 0.7 + avg_score × 0.3
6. 输出：{answer, citations, confidence}
```

**置信度公式**：
```
confidence = max(scores) × 0.7 + avg(scores) × 0.3
```
其中 `scores` 为检索到的 Top-3 文档的向量相似度分数。该公式综合考虑"最相关片段"和"整体相关性"，既不过于乐观也不过于保守。

---

## 五、技术路线

### 5.1 开发路线

```
第一阶段：核心 RAG 链路（4 天）
├── FastAPI 项目骨架 + SQLite/SQLAlchemy 模型
├── 多格式文档解析 (PDF/PPTX/DOCX/MD/TXT)
├── 智能分块 + BGE-M3 向量化 + Qdrant 存储
└── 向量检索 + LLM 答案生成 + 引文标注

第二阶段：前端与体验优化（3 天）
├── Next.js 14 Chat UI (Tailwind CSS)
├── SSE 流式回答推送
├── 课程管理 + 文档上传界面
└── 引用面板 + 对话历史

第三阶段：工程化与发布（2 天）
├── Docker Compose 一键部署
├── PyInstaller EXE 打包
├── 端口检测 / Qdrant 本地锁 / 配置容错
└── GitHub Release 分发 + README 文档
```

### 5.2 未来扩展路线

| 方向 | 内容 | 优先级 |
|------|------|--------|
| 关键词检索 | BM25 与向量检索并行，融合排序（RFF） | 高 |
| 图片 OCR | PDF/PPT 内嵌图片文字提取 | 中 |
| 自适应出题 | 根据知识库自动生成选择题/简答题 | 中 |
| 语音交互 | 集成 GPT-SoVITS TTS 语音朗读答案 | 低 |
| 知识图谱 | 课程概念关系图谱可视化 | 低 |

---

## 六、创新亮点

### 6.1 Smart Hybrid 三级回答策略

业界常见 RAG 实现通常走两个极端：
- **严格模式**：资料中没有就不回答（"未找到相关内容"），用户体验差；
- **放任模式**：LLM 自由发挥，不加区分地混合资料和模型知识，学生不知道哪句话来自课件。

课答独创**三级自适应策略**：
1. 资料相关 → 逐句标注来源编号
2. 资料不够详细 → 以资料为主干，LLM 知识自然填充背景、推导、类比（无需生硬标签）
3. 资料完全不相关 → 直接以通用知识回答，不强行拉扯资料

这一策略在 System Prompt 中通过精心设计的指令模板实现，保持了语义的自然流畅，避免了传统 RAG 中"生硬引用"和"资料盲区沉默"的问题。

### 6.2 语义边界智能分块

课答的分块算法不依赖 LangChain 等重量级框架，自行实现了**基于文档结构感知的语义分块**：

- 识别 PDF 页码、PPT 幻灯片号等**结构标记**，作为自然的分块边界
- 以段落为最小切割单元，绝不跨段落截断
- 维护 chunk 起始页码，确保回答中"第 X 页"的引用准确对应原文位置

### 6.3 零安装门槛的 EXE 交付

将 Python 后端（FastAPI + Uvicorn + Qdrant + SQLAlchemy）与 Next.js 静态前端打包为单个 67MB 的 Windows 可执行文件：

- **技术难点突破**：
  - venv 隔离打包：`--collect-all` 确保 numpy / opencv / pymupdf 等 C 扩展正确包含；
  - GBK 编码兼容：Windows CMD 中文输出不崩溃；
  - SPECPATH 基准路径：`_MEIPASS` 与 `sys.executable` 双路径策略；
  - EXE 版本 `.env` 加载顺序：运行时优先读取 EXE 同级 `.env`，其次环境变量、最后代码默认值；
  - Qdrant 本地模式仅支持单进程（portalocker 文件锁），通过端口检测防止二次启动崩溃，友好提示"已在运行中"。
- **最终效果**：普通用户（非开发者）下载 ZIP → 填好 API Key → 双击 EXE → 浏览器自动打开，3 分钟上手。

### 6.4 双模式数据库架构

独创的"本地/容器双模"数据架构：

```
开发 / EXE 一键版          Docker 生产部署
┌──────────┐              ┌────────────┐
│  SQLite  │              │ PostgreSQL │
│ (本地文件)│              │  (容器)    │
├──────────┤              ├────────────┤
│  Qdrant  │              │  Qdrant    │
│ (本地模式)│              │  (容器)    │
└──────────┘              └────────────┘
```

通过 `config.py` 中的条件判断自动切换：
- 检测到 `QDRANT_URL` 环境变量 → 连容器；
- 未检测到 → 本地文件模式。
- 同一套代码零修改，由环境变量驱动切换。

### 6.5 国内网络环境适配

- HuggingFace 模型下载：自动设置 `HF_ENDPOINT=https://hf-mirror.com` 镜像
- Git Bash MSYS 路径转换修复：前端 API 地址改为运行时检测（端口 8000 → `/api`），避免构建期 Git Bash 把 `/api` 误转为 `E:/Git/api`
- Embedding 服务走国内硅基流动，延迟低、不被墙

---

## 七、应用场景

### 7.1 高校课堂辅助

- **场景**：学生在课后复习数据结构、数据库原理等课程时，上传教师课件（PPT/PDF）和教材
- **使用方式**：针对课件中某页的算法步骤提问，系统回答时标注来自哪份课件、第几页
- **效果**：学生无需翻找多个文件，聊天式提问即可获得有出处的精确解答

### 7.2 期末复习备考

- **场景**：学生将一学期的课件、教材章节、习题笔记上传到同一课程
- **使用方式**：跨文档提问、概念对比（如"对比 B 树和 B+ 树的区别"），系统自动综合多文档信息
- **效果**：AI 从零散资料中提取知识脉络，辅助构建知识体系

### 7.3 研究生文献研读

- **场景**：将学术论文 PDF 批量上传，快速理解关键技术细节
- **使用方式**：问"这篇论文的方法论是什么？""与 XXX 方法的主要区别？"
- **效果**：跨论文检索，辅助文献综述写作

### 7.4 职业培训与终身学习

- **场景**：企业内部培训材料、技术文档的知识库
- **使用方式**：新员工入职时上传培训手册，快速上手；技术文档变更后重新上传即可更新知识库
- **效果**：降低培训人力成本，24/7 自助答疑

---

## 八、合规伦理说明

### 8.1 数据隐私保护

- **完全本地化存储**：EXE 版本中，所有数据（SQLite 数据库、Qdrant 向量索引、上传文件）全部存储在用户本地磁盘，**不上传任何数据至第三方服务器**；
- **仅调用云 API**：向外发送的仅有经过向量化后的 1024 维浮点数（用于 Embedding 和 LLM 推理），不包含原始文档内容；
- **用户完全掌控**：删除课程即物理清除关联文件和数据库记录；关闭/删除程序目录即完全清除所有数据。

### 8.2 学术诚信

- **引文溯源机制**：每条答案句末标注来源编号，末尾列出参考来源清单（文档名 + 页码），帮助学生在引用 AI 回答时追溯到原始资料；
- **辅助而非替代**：系统定位为"学业辅导助手"，鼓励学生先尝试自主思考，再用 AI 验证理解、拓展知识面——不鼓励直接复制答案应对作业或考试；
- **防滥用设计**：答案长度上限 2048 tokens，避免生成完整论文/作业；temperature 设置在 0.3（偏确定性），减少随机发散生成。

### 8.3 知识产权

- **用户资料归用户所有**：系统不上传、存储、分析用户上传的课程资料用于除当前用户的答疑之外的任何用途；
- **开源合规**：项目依赖的第三方库（PyMuPDF、python-pptx、Qdrant、FastAPI 等）均使用 MIT/Apache-2.0 等宽松开源许可证，无 GPL 传染风险；
- **AI 生成内容声明**：回答中涉及非课程资料的知识补充来自 LLM 训练数据，系统通过引文标注机制明确区分"来自资料"与"模型生成"。

### 8.4 伦理使用边界

- **非医疗/法律场景**：课答定位为通用学业辅导工具，不针对医疗诊断、法律建议等高风险领域进行优化或宣称支持；
- **无用户画像**：系统不追踪用户个人身份，不构建用户行为画像，不向用户推送广告或商业内容；
- **年龄适用性**：面向高中及以上教育场景，未成年用户建议在教师或家长指导下使用。

### 8.5 技术伦理

- **模型偏见认知**：系统使用的 DeepSeek-V3 和 BGE-M3 均为预训练大模型，可能包含训练数据中的社会偏见。课答通过引文溯源机制（优先基于用户自有资料回答）在一定程度上缓解了模型幻觉和偏见问题；
- **可解释性**：置信度分值（0-1）帮助用户评估回答可靠性，低分回答建议用户核验资料原文；
- **持续改进**：通过用户点赞/点踩反馈机制收集回答质量数据（匿名），用于后续优化检索和生成策略。

---

## 附录 A：API 接口概览

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/courses/` | 创建课程 |
| `GET` | `/api/courses/` | 课程列表（含文档数/分块数统计） |
| `GET` | `/api/courses/{id}` | 课程详情 |
| `DELETE` | `/api/courses/{id}` | 删除课程（级联删除文档+向量） |
| `POST` | `/api/documents/upload` | 上传文档（自动解析→分块→向量化） |
| `POST` | `/api/chat/ask` | RAG 答疑（完整响应含答案+引文+置信度） |
| `POST` | `/api/chat/ask/stream` | 流式 RAG 答疑（SSE，逐 token 推送） |
| `GET` | `/api/conversations/` | 会话列表 |
| `POST` | `/api/feedback/` | 回答质量反馈（点赞/点踩） |

## 附录 B：技术栈版本

| 组件 | 版本 | 许可 |
|------|------|------|
| Python | 3.12 | PSF |
| FastAPI | 0.115.x | MIT |
| Next.js | 14.2 | MIT |
| Qdrant Client | 1.18.x | Apache 2.0 |
| DeepSeek API | deepseek-chat | 商业 |
| BGE-M3 (via SiliconFlow) | — | 商业 API |
| PyMuPDF | 1.25.x | AGPL（仅调用，非修改源码） |
| PyInstaller | 6.x | GPL（仅构建工具，不进入产物） |
